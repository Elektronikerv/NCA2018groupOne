import { Address } from "./address.model";


export class Office {
    id: number;
    name: string;
    description: string;
    address: Address;

  //   fullAddress(): string {
  //     return this.address.house + ' ' + this.address.street + ' ' + this.address.floor + ' Fl ' + this.address.flat + ' Apt ';
  // }
  }